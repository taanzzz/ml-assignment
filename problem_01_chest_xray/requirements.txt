# Problem 01 - Chest X-ray CNN (NORMAL vs PNEUMONIA)
#
# Exact versions of the environment in which the reported experiment was run
# (Google Colab; run ID 20260905T121522Z_f27df8; see results/environment.json
# and results/run_config.json for the frozen run configuration).
#
# Note for Colab users: the standard runtime already ships compatible
# versions, so no pip install is normally needed inside the notebook.
# This file is for reproducible local environments.

tensorflow==2.20.0
numpy==2.1.3
pandas==2.2.3
scikit-learn==1.6.1
matplotlib==3.10.0
pillow==11.3.0
tqdm==4.67.3
